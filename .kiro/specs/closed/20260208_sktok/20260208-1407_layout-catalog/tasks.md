# Tasks: layout-catalog

## Phase 1: 基盤 + 高優先度レイアウト
- [x] tech.md に component/pattern 役割分離ガイドを追記
- [x] layout-catalog.md 作成（frontmatter + 冒頭説明）
- [x] columns（2/3/4列）座標テーブル + スケルトンJSON + 検証
- [x] split（50:50, 60:40, 70:30）座標テーブル + スケルトンJSON + 検証
- [x] grid-2x2 座標テーブル + スケルトンJSON + 検証
- [x] rows（2/3/4行）座標テーブル + スケルトンJSON + 検証
- [x] timeline（3〜6ステップ）座標テーブル + スケルトンJSON + 検証
- [x] process-flow（3〜5ステップ）座標テーブル + スケルトンJSON + 検証

## Phase 2: 中優先度レイアウト
- [x] bento（大1+小2, 大1+小3）座標テーブル + スケルトンJSON + 検証
- [x] hero-body 座標テーブル + スケルトンJSON + 検証
- [x] centered 座標テーブル + スケルトンJSON + 検証
- [x] comparison 座標テーブル + スケルトンJSON + 検証
- [x] funnel（3〜5段）座標テーブル + スケルトンJSON + 検証
- [x] pyramid（3〜5段）座標テーブル + スケルトンJSON + 検証

## Phase 3: 低優先度レイアウト
- [x] cycle（3〜6ノード）座標テーブル + スケルトンJSON + 検証
- [x] centric（4〜6ノード）座標テーブル + スケルトンJSON + 検証
- [x] venn（2円, 3円）座標テーブル + スケルトンJSON + 検証
- [x] dashboard（2+2, 1+3）座標テーブル + スケルトンJSON + 検証
- [x] full-bleed 座標テーブル + スケルトンJSON + 検証

## Validation
- [x] 全レイアウトのスケルトンJSONがgenerate + previewで正常表示
- [x] コミット + push

---
**Created**: 2026-02-08
